#!/bin/bash

# ========================================
# Script to compile SCSS files to CSS files with watch mode
# ========================================

# Make sure sass is installed. It can be installed with `npm install -g sass`
sass --watch ../css/:../css
